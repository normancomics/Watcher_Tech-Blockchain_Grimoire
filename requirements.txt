# ⛧ Watcher Tech Blockchain Grimoire — Python Dependencies

# MCP server framework
mcp>=1.0.0

# Blockchain interaction (optional — required for on-chain tools)
web3>=7.0.0

# Ephemeris / planetary alignment (optional — falls back to heuristic)
ephem>=4.1.6

# Environment variable loading
python-dotenv>=1.0.0
